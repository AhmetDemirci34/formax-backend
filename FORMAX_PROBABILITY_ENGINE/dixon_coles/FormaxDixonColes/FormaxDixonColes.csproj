<Project Sdk="Microsoft.NET.Sdk">

  <!-- Research / backtest tool. NOT added to Formax.slnx, no NuGet packages, no production wiring. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>FormaxDixonColes</AssemblyName>
    <RootNamespace>Formax.DixonColes</RootNamespace>
    <InvariantGlobalization>true</InvariantGlobalization>
  </PropertyGroup>

  <ItemGroup>
    <!-- reuse the match reader and models of the team strength engine -->
    <ProjectReference Include="..\..\team_strength\FormaxTeamStrength\FormaxTeamStrength.csproj" />
  </ItemGroup>

</Project>
